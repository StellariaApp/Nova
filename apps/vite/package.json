{
  "name": "@stellaria/supernova-web-vite",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "clean": "rimraf -rf .turbo && rimraf -rf node_modules && rimraf -rf dist",
    "dev": "vite",
    "lint": "eslint --ext .js,.jsx,.ts,.tsx .",
    "start": "vite preview"
  },
  "dependencies": {
    "@stellaria/supernova": "*",
    "jotai": "^2.4.3",
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "eslint-stellaria-supernova": "*",
    "tsconfig": "*",
    "typescript": "^5.0.2"
  }
}
