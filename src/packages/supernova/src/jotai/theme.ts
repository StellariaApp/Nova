import { atom } from 'jotai';

export const ThemeAtom = atom({
  default: null,
  theme: null,
  themes: null,
  key: null,
  keys: null,
  set: () => null,
  get: () => null,
  toggle: () => null
});
