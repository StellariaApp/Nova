const dark = {
  colors: {
    primary: '#000000',
    secondary: '#ffffff'
  }
};

const light = {
  colors: {
    primary: '#ffffff',
    secondary: '#000000'
  }
};

const themes = {
  dark,
  light
};

export default themes;
